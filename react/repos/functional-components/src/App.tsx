/**
 * Placeholder starting point.
 *
 * This is a coaching starter, not a finished app. Do NOT ask the AI to fill in
 * the whole Weather Dashboard here — read TASK.md, plan the approach with your
 * assistant, and build it in small, understood steps. Replace this component as
 * you go, and record each decision in CHANGELOG.md.
 */
export function App() {
  return (
    <main>
      <h1>Weather Dashboard — start here</h1>
      <p>
        Open <code>TASK.md</code> for the requirements, then work through them with your AI
        assistant as a coach (see <code>CLAUDE.md</code>). Log every decision in{' '}
        <code>CHANGELOG.md</code>.
      </p>
    </main>
  );
}
