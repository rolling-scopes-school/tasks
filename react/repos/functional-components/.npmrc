# This project is intended to be used with pnpm (see the `packageManager` field
# in package.json; run `corepack enable` to use the pinned version).
#
# `engines` in package.json is advisory only — we don't set engine-strict, so a
# slightly older/newer Node won't hard-block installs.
auto-install-peers=true
